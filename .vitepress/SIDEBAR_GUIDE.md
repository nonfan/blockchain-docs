# Sidebar 配置说明（智能自动版）

本文档说明基于三层回退机制的智能 `.vitepress/sidebar.ts` 配置方式。

## 📁 核心设计理念

新的 sidebar 采用**智能三层回退机制**：
- ✅ 优先读取 frontmatter 的 `title` 字段
- ✅ 其次提取 markdown 的 `# 标题`
- ✅ 最后智能解析文件名
- ✅ 支持数字前缀排序（如 `01_basic.md`）
- ✅ 支持 frontmatter 的 `order` 字段控制顺序
- ✅ 最小化手动配置，几乎零维护

## 🎯 工作原理

### 1. 三层回退机制

```typescript
// 标题提取优先级
1. Frontmatter (title)  → 最高优先级
2. 文件 H1 标题          → 中等优先级
3. 智能文件名解析        → 兜底方案
```

**示例：**

```markdown
---
title: 快速开始
order: 1
---

# Quick Start

内容...
```

- ✅ 显示为 "**快速开始**"（来自 frontmatter）
- ✅ 排序权重为 **1**（来自 frontmatter）

如果没有 frontmatter：
```markdown
# 对象模型

内容...
```
- ✅ 显示为 "**对象模型**"（来自 H1）

如果什么都没有：
```markdown
文件名：01_quick-start.md
```
- ✅ 显示为 "**Quick Start**"（智能解析）

### 2. 排序机制

**优先级：**
1. **数字前缀** - `01_`, `02_`, `03_`...
2. **frontmatter order** - `order: 1`
3. **字母顺序** - 默认排序

**示例文件结构：**
```
docs/sui/
├── intro.md                    # frontmatter: order: 1
├── 02_quick-start.md          # 数字前缀: 2
├── advanced.md                # 字母顺序
└── move/
    ├── 01_basic.md            # 数字前缀: 1
    ├── 02_object.md           # 数字前缀: 2
    └── 03_capability.md       # 数字前缀: 3
```

**生成的顺序：**
```
Sui
├── Sui (intro.md - order: 1)
├── Quick Start (02_quick-start.md - 数字 2)
├── Advanced (advanced.md - 字母 a)
└── Move
    ├── Move 语言基础 (01_basic.md)
    ├── 对象模型 (02_object.md)
    └── 能力系统与设计模式 (03_capability.md)
```

## 📝 使用方式

### 方式 1：使用 Frontmatter（推荐）

在文件顶部添加 YAML frontmatter：

```markdown
---
title: 快速开始指南
order: 2
---

# Quick Start

你的内容...
```

**优点：**
- ✅ 完全控制显示文本
- ✅ 灵活控制排序
- ✅ 支持中英文混合
- ✅ 标准化、可扩展

### 方式 2：使用 H1 标题（常用）

直接在文件中使用 H1 标题：

```markdown
# 快速开始

你的内容...
```

**优点：**
- ✅ 无需额外配置
- ✅ 自然的 markdown 写法
- ✅ 标题和内容一致

### 方式 3：依赖文件名（兜底）

创建文件时使用有意义的文件名：

```
02_quick-start.md  → Quick Start
01_intro.md        → Intro (或映射为"公链概览")
```

**优点：**
- ✅ 零配置
- ✅ 自动生成
- ✅ 适合英文文档

## 🔧 智能解析规则

### 文件名解析

```typescript
// 自动转换规则
'01_quick-start.md'  → 'Quick Start'
'hello-world.md'     → 'Hello World'
'my_doc.md'          → 'My Doc'
'API_reference.md'   → 'API Reference'
```

**特殊映射（可选）：**
```typescript
const specialCases = {
  'intro': '公链概览',
  'quick-start': '快速开始'
}
```

只需为**常用的特殊词汇**添加映射，其他自动处理。

### 文件夹名称解析

```typescript
// 文件夹映射
'move'        → 'Move'
'sdks'        → 'SDKs'
'token'       → '代币'
'ecosystem'   → '生态标准与工具链'

// 自动转换
'my-folder'   → 'My Folder'
'api_docs'    → 'Api Docs'
```

## 📊 常见场景

### 场景 1️⃣：新增一篇文章

**无需修改 sidebar.ts！**

1. 创建文件：`docs/sui/advanced-topics.md`
2. 添加标题：
   ```markdown
   ---
   title: 高级主题
   order: 10
   ---

   # Advanced Topics

   内容...
   ```
3. 刷新页面，自动显示 ✅

### 场景 2️⃣：控制文章顺序

**方法 1：使用数字前缀**
```
docs/sui/
├── 01_intro.md
├── 02_quick-start.md
├── 03_advanced.md
```

**方法 2：使用 frontmatter**
```markdown
---
order: 5
---
```

**方法 3：组合使用**
```
01_intro.md + frontmatter order: 1
```

### 场景 3️⃣：中英文混合

**推荐方式：**

```markdown
---
title: 快速开始
---

# Quick Start

支持中文标题 + 英文文件名
```

**文件名：** `quick-start.md` （利于 URL）
**显示名：** "快速开始"（frontmatter）

### 场景 4️⃣：新增一个公链

1. 创建文件夹和文件：
   ```
   docs/avalanche/
   ├── intro.md
   └── subnet.md
   ```

2. 在 `sidebar.ts` 中添加（只需一行）：
   ```typescript
   {
     text: 'Avalanche',
     collapsed: true,
     icon: 'avalanche.svg',
     items: scanChain('avalanche')  // ✨ 自动扫描
   }
   ```

3. 完成！所有文件自动生成导航 ✅

### 场景 5️⃣：处理特殊词汇

如果有特殊的技术词汇需要自定义显示：

**选项 1：使用 frontmatter（推荐）**
```markdown
---
title: NFT
---

# NFT

内容...
```

**选项 2：添加到映射表**
```typescript
// sidebar.ts 中的 specialCases
const specialCases = {
  'nft': 'NFT',
  'api': 'API',
  'sdk': 'SDK'
}
```

## ⚙️ 配置文件说明

### 主要函数

#### `extractTitle(filePath: string): string`

提取文件标题（三层回退）：
1. 解析 frontmatter 的 `title`
2. 提取 markdown 的 `# 标题`
3. 智能解析文件名

#### `smartParseFilename(filename: string): string`

智能解析文件名：
```typescript
'01_quick-start.md'  → 'Quick Start'
'hello-world.md'     → 'Hello World'
```

#### `smartParseFolderName(folderName: string): string`

智能解析文件夹名称：
```typescript
'my-folder'  → 'My Folder'
'sdks'       → 'SDKs'  // 特殊映射
```

#### `getSortOrder(name: string, filePath?: string): number`

获取排序权重：
1. 从文件名提取数字前缀（`01_` → `1`）
2. 从 frontmatter 读取 `order`
3. 默认 `999`

#### `scanChain(chainName: string): SidebarItem[]`

扫描公链文件夹并生成 sidebar：
```typescript
scanChain('sui')  // 自动扫描 docs/sui/ 下所有文件
```

## 🌟 最佳实践

### 1. Frontmatter 规范

```markdown
---
title: 快速开始指南        # 显示标题
order: 1                  # 排序权重（可选）
description: 这是描述     # 页面描述（可选）
---

# Quick Start

内容...
```

### 2. 文件命名规范

```
✅ 推荐：
- 01_intro.md
- 02_quick-start.md
- hello-world.md

❌ 避免：
- 快速开始.md          # 避免中文文件名
- my file.md           # 避免空格
- File-Name.md         # 避免大写（URL 不友好）
```

### 3. 文件夹结构规范

```
✅ 清晰的层次：
docs/sui/
├── intro.md            # 公链介绍
├── quick-start.md      # 快速开始
└── move/               # Move 语言
    ├── 01_basic.md
    ├── 02_object.md
    └── 03_capability.md

❌ 过深的嵌套：
docs/sui/
└── advanced/
    └── topics/
        └── deep-dive/
            └── tutorial.md  # 太深了
```

### 4. 标题一致性

**推荐：Frontmatter + H1 保持一致**

```markdown
---
title: 快速开始
---

# 快速开始

内容...
```

或使用英文文件名 + 中文标题：

```markdown
---
title: 快速开始
---

# Quick Start

中文内容...
```

## 🔍 故障排查

### 问题 1：文章没有显示在 sidebar

**可能原因：**
- 文件不在 `docs/` 目录下
- 文件名是 `index.md`（会被忽略）
- 文件扩展名不是 `.md`

**解决方案：**
1. 检查文件路径
2. 重命名文件
3. 刷新页面

### 问题 2：标题显示不正确

**可能原因：**
- frontmatter 格式错误
- H1 标题格式错误（需要 `# 标题`）
- 文件名无法智能解析

**解决方案：**
1. 检查 frontmatter 格式（YAML）
2. 确保 H1 标题正确：`# 标题`
3. 添加特殊映射或使用 frontmatter

### 问题 3：文章顺序不对

**可能原因：**
- 没有使用数字前缀
- frontmatter order 冲突

**解决方案：**
1. 使用数字前缀：`01_`, `02_`, `03_`
2. 或在 frontmatter 中指定 `order`
3. 确保不同文件的 order 不冲突

### 问题 4：特殊词汇显示错误

**例如：** `nft.md` 显示为 "Nft" 而不是 "NFT"

**解决方案：**

**方法 1：使用 frontmatter**
```markdown
---
title: NFT
---
```

**方法 2：添加到映射表**
```typescript
// sidebar.ts - specialCases
'nft': 'NFT',
'api': 'API'
```

## 🚀 进阶技巧

### 1. 动态生成配置

如果需要更复杂的配置，可以创建独立配置文件：

```typescript
// .vitepress/sidebar-config.ts
export const SPECIAL_TITLES = {
  'intro': '公链概览',
  'quick-start': '快速开始',
  'nft': 'NFT',
  'api': 'API'
}

export const FOLDER_TITLES = {
  'move': 'Move',
  'sdks': 'SDKs',
  'token': '代币'
}
```

然后在 `sidebar.ts` 中导入：
```typescript
import { SPECIAL_TITLES, FOLDER_TITLES } from './sidebar-config'
```

### 2. 多语言支持

如果需要支持多语言：

```markdown
---
title: 快速开始
title_en: Quick Start
lang: zh-CN
---
```

修改 `extractTitle` 函数：
```typescript
function extractTitle(filePath: string, locale?: string): string {
  // ...
  if (locale === 'en' && data.title_en) {
    return data.title_en
  }
  if (data.title) {
    return data.title
  }
  // ...
}
```

### 3. 条件显示

在 frontmatter 中添加条件：

```markdown
---
title: 高级功能
draft: true
---
```

修改 `scanFolder` 跳过 draft 文件：
```typescript
if (entry.name.endsWith('.md')) {
  const { data } = matter(fs.readFileSync(fullPath, 'utf-8'))
  if (data.draft) {
    continue  // 跳过草稿
  }
}
```

## 📚 总结

新的智能系统的优势：

1. **零维护** - 大部分文件无需配置
2. **灵活配置** - frontmatter 支持完全控制
3. **自动排序** - 数字前缀或 order 字段
4. **智能解析** - 文件名自动转换为标题
5. **易于扩展** - 新增公链只需一行代码
6. **标准化** - 使用 frontmatter 行业标准

**推荐工作流：**
1. ✅ 创建文件，添加 H1 标题
2. ✅ 需要自定义时添加 frontmatter
3. ✅ 使用数字前缀控制顺序
4. ✅ 特殊词汇添加到小型映射表

**维护者：** 项目团队
**最后更新：** 2025-01-28
